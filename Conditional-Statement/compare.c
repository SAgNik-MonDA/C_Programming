#include <stdio.h>
int main(){
    int x=80,y=50;
    if(x==y)
    printf("x is equal to y");
    else if(x>y)
    printf("x is grater than y");
    else if (x<y)
    printf("x is less  than y"); 
    return 0;
}